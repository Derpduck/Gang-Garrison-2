<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="DRAW" id="0">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">if (instance_exists(DeathCam))
    exit;

//
xoffset = view_xview[0];
yoffset = view_yview[0];
//xsize = view_wview[0];
//ysize = view_hview[0];

// Build text
var rows;
rows = 0;

var killerName, killerClass, killerHP;

if (killer.object == -1)
    //
else if (killer == global.myself)
    //

var xbegin, ybegin, width, height;
// TODO: Adjust with timer position, currently accounts for left aligned timer + stacked KOTH timers
xbegin = xoffset + 36;
ybegin = yoffset + 108;
width = 100;
height = 100;

draw_set_alpha(0.5);
draw_set_color(c_black);

draw_rectangle(xbegin, ybegin, xbegin + width, ybegin + height, true);

// Add border
</argument>
      </arguments>
    </action>
  </actions>
</event>
