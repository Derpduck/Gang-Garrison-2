// Create DSM directories
if (!directory_exists(working_directory + "\DSM"))
    directory_create(working_directory + "\DSM");

if (!directory_exists(working_directory + "\DSM\Logs"))
    directory_create(working_directory + "\DSM\Logs");
