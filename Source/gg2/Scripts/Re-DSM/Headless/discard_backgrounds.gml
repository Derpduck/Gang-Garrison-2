// Remove all backgrounds from memory
// They still seem to render so doesn't seem to do much
background_delete(MenuBackground1);
background_delete(MenuBackground2);
background_delete(MenuBackground3);
background_delete(SplashScreenB);
background_delete(BuilderBackground);
